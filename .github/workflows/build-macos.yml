name: Build on macOS

on: [push]

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v2
      - name: Setup
        run: brew install automake
      - name: Build
        run: ./build.sh
